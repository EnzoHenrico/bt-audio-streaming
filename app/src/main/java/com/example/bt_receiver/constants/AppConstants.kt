package com.example.bt_receiver.constants

class AppConstants {
    object  ALERT {
        object BT {
            const val ENABLE = "Bluetooth enable"
            const val NOT_ENABLE = "Device Bluetooth in not enable"
            const val NOT_FOUND = "Bluetooth adapter not found"
        }
    }
}